# Podfile

# Select the appropriate platform below
# Specify the minimum supported iOS version (or later) required by Kiwi
platform :ios, '8.0'
# platform :osx

#
# Add Kiwi as an exclusive dependency for the MHVLibTests target
#
target :MHVLibTests do
  pod 'Kiwi'
end